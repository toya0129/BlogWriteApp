.menu
  - if login_check
    = button_to "topへ", root_path , :class => 'button', :method => :get
    = button_to "記事を投稿する", new_article_path,:class => 'button', :method => :get
    = button_to "つぶやく", new_tweet_path,:class => 'button', :method => :get
    = button_to "ログアウトする", logout_path, :class => 'button', :method => :get
  - else
    = button_to "アカウント登録", sign_up_path,:class => 'button', :method => :get
    = button_to "ログイン", login_path,:class => 'button', :method => :get
